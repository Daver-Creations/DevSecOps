pipeline {
    agent any
    stages {
        stage('Verify Docker') {
            steps {
                retry(3) {
                    echo "Verifying Docker"
                }
                sleep(10)
                sh "docker --version"
            }
        }
        // stage('Build Docker Image') {
        //     steps {
        //         sh ""
        //     }
        // }
    }
}